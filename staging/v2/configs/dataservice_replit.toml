# DataService (saa-core-data-v2) Replit Configuration
# Protocol: AGENT3_HANDSHAKE v30

run = "python src/main.py"
entrypoint = "src/main.py"

[deployment]
run = ["python", "src/main.py"]
deploymentTarget = "autoscale"

[env]
PORT = "5000"

[nix]
channel = "stable-24_05"

[[ports]]
localPort = 5000
externalPort = 80
